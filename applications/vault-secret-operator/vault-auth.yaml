apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: default

spec:
  vaultConnectionRef: vault
  method: kubernetes
  mount: kubernetes

  kubernetes:
    role: external-secrets-role
    serviceAccount: external-secrets-vault