---
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultDynamicSecret
metadata:
  name: artifactory-token
spec:
  vaultAuthRef: default
  mount: kv
  path: artifactory-secret

  destination:
    create: true
    name: artifactory-dynamic-secret

  renewalPercent: 67